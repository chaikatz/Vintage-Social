# VINTAGE — PRODUCTION
#
# Copy this to .env.production and fill in the two values from
# Supabase → Vintage-Production → Project Settings → API.
#
# .env.production is gitignored: production is where the real members live,
# and its configuration does not belong in the repository even though the
# anon key is publishable.
#
# Switch with:  npm run use:production
EXPO_PUBLIC_SUPABASE_URL=https://scfwowqsqrnzpknzurmm.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=
EXPO_PUBLIC_DEMO_MODE=0
